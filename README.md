# Swap-Using-XOR
This C++ program swaps two numbers without using a third variable by applying the XOR (^) bitwise operator. It is an efficient and safe method that avoids the overflow issue present in arithmetic swapping.
